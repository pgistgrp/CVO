/*
 * Bean Shell Script.
 * Testing script for hibernate execution.
 */

categoryId = 4290;
Long cctId = 3342;

hql = "from Activity";

query = session.createQuery(hql);
//query.setLong(0, 3342);

v = session.load(org.pgist.users.Vehicle.class, new Long(11431));
print(v.getApproxValue());
print(v.getOwner());

List test = query.list();

