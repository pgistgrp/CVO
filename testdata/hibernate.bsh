/*
 * Bean Shell Script.
 * Testing script for hibernate execution.
 */

categoryId = 4290;
Long cctId = 3342;

hql = "from PackageVoteSuite where id=?";

query = session.createQuery(hql);
query.setLong(0, 3342);


List test = query.list();

for (Object o : test) {
    System.out.println("-----> "+o.stats.size());
}//for

o = session.load(org.pgist.packages.PackageVoteSuite.class, cctId);
System.out.println("-----> "+o.stats.size());

for (x : o.stats) {
	print(x.id);
}
