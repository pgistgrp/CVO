/*
 * Bean Shell Script.
 * Testing script for hibernate execution.
 */

query = session.createQuery("from User u where u.id in (select indices(cst.categories) from CST cst where cst.id=?) and u.id<>? order by u.loginname");
query.setLong(0, 1515);
query.setLong(1, 5);
list = query.list();
for (Object one : list) {
    print(one.id);
}

